[build]
builder = "nixpacks"
buildCommand = "pip install -r requirements.txt && python seed_database.py"

[deploy]
startCommand = "uvicorn app:app --host 0.0.0.0 --port 8000"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
