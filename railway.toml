[build]
builder = "nixpacks"
buildCommand = "pip install -r requirements.txt && python seed_database.py"

[deploy]
startCommand = "uvicorn api.main_db:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
